import Button from "../Button/Button";

const UpdateComment = () => {
  return (
    <form>
      <input col="30" row="10" />
      <Button>update</Button>
    </form>
  );
};

export default UpdateComment;
